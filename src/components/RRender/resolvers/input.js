import React from 'react';
import {default as  BaseResolver} from './base';

export default  class InputResolver extends BaseResolver   {
  constructor(dataSource, template) {
   super(dataSource, template);
  }
}
